fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
pydantic==2.10.4
jinja2==3.1.5
python-multipart==0.0.20

# Identidad del panel (sustituye la clave unica compartida)
itsdangerous==2.2.0
argon2-cffi==25.1.0
# El extra [requests] trae el transporte HTTP que google-auth usa para descargar las claves
# publicas de Google al verificar un identificador. Se usa el transporte que la propia
# biblioteca prueba, en vez de uno propio sobre httpx: no es sitio para hacerlo a mano.
google-auth[requests]==2.58.0

# Entregas a medida (002). El PDF que exporta Canva se convierte en las imagenes del carrusel.
#   pypdfium2 y NO PyMuPDF: PyMuPDF es AGPL y este repositorio es publico sin esa licencia
#   (constitucion, principio V). pypdfium2 es BSD-3/Apache-2.0 y ademas lee el texto de cada
#   pagina, que es lo que permite rotular la miniatura y avisar de que lleva un precio.
pypdfium2==5.9.0
Pillow==12.2.0
